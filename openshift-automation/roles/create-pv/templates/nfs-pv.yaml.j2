apiVersion: v1
kind: PersistentVolume
metadata:
  name: {{ pv_name }} 
spec:
  capacity:
    storage: {{ pv_size }}
  accessModes:
  - {{ pv_access_mode }}
  nfs: 
    path: {{ nfs.path }}/{{ pv_name }}
    server: {{ nfs.ip }}
  persistentVolumeReclaimPolicy: Retain 